# حالة ربط خدمات Firebase في تطبيق كتب FM (Firebase Integration Status)

يحتوي هذا الملف على تفصيل دقيق لجميع الأجزاء والخدمات التي تم ربطها بقاعدة البيانات والخدمات الحقيقية لـ **Firebase** و **Gemini**، مقارنة بالخدمات التي لا تزال تعمل ببيانات وهمية (Mock Data).

---

## 🟢 أولاً: الأجزاء المرتبطة بـ Firebase حقيقية (Real Firebase Connections)

### 1. نظام الحسابات والمصادقة (Authentication & Users)
* **الحالة:** مكتمل بالكامل بربط حقيقي مع **Firebase Auth** و **Firestore** و **Firebase Storage**.
* **العمليات المدعومة:**
  * **البريد الإلكتروني:** التسجيل وإنشاء الحساب (`createUserWithEmailAndPassword`)، تسجيل الدخول (`signInWithEmailAndPassword`)، إرسال رابط تفعيل البريد تلقائياً عند التسجيل (`sendEmailVerification`)، وإعادة تعيين كلمة المرور (`sendPasswordResetEmail`).
  * **التسجيل الاجتماعي (Google):** تسجيل الدخول المباشر بحساب جوجل واستخلاص البيانات (`GoogleSignIn` + `GoogleAuthProvider`).
  * **تسجيل الدخول برقم الهاتف (OTP):** إرسال رمز التحقق SMS على الهاتف عبر Firebase، التحقق من الرمز، الدخول التلقائي في حال قراءة الرمز تلقائياً (`verifyPhoneNumber` و `signInWithCredential`).
  * **حساب الزائر:** الدخول كحساب مجهول (`signInAnonymously`).
  * **الربط مع Firestore:** عند التسجيل لأول مرة، يتم إنشاء وثيقة مستخدم حقيقية في مجموعة `/users/{uid}` تخزن فيها بياناته (الاسم، البريد، نوع الحساب، طريقة التحقق، وتاريخ الإنشاء).
  * **تخزين الصور (Firebase Storage):** عند التسجيل أو تعديل الصورة الشخصية، يتم رفع ملف الصورة محلياً على Storage وحفظ رابط التحميل المباشر (`photoUrl`) في وثيقة المستخدم.

### 2. الشاشة الشخصية وتتبع النشاط (Profile & Activity Tracker)
* **الحالة:** مكتمل بالكامل بربط حقيقي مع **Firestore** و **Firebase Auth**.
* **العمليات المدعومة:**
  * **المزامنة اللحظية:** التطبيق يستمع لحظياً للتغييرات في وثيقة المستخدم عبر السنابشوت (`snapshots().listen`) لتحديث إحصائيات النشاط فوراً.
  * **البيانات الشخصية الحية:** جلب وتحديث (الاسم، السيرة الذاتية Bio، التصنيفات المفضلة، قائمة الاستماع الحالية Continue Listening).
  * **تحديث البريد الإلكتروني الأمني:** استخدام آلية التحقق قبل تحديث البريد الإلكتروني (`verifyBeforeUpdateEmail`) لمنع تعديله بدون تأكيد.

### 3. الشاشة الرئيسية والتوصيات (Home & Recommendations)
* **الحالة:** مكتمل بالكامل بربط حقيقي مع **Firestore**.
* **العمليات المدعومة:**
  * **التصنيفات المخصصة:** جلب التصنيفات المفضلة للمستخدم الحالي من `/users/{uid}/favoriteCategoryIds` ومطابقتها بالتصنيفات الحقيقية في مجموعة `/categories`.
  * **الكتب المقترحة:** جلب الكتب النشطة من مجموعة `/books` (بشرط `isActive == true`) كبيانات حقيقية بالكامل (العنوان، الكاتب، الغلاف، التقييم، والمدة).

### 4. تفاصيل الكتب والفصول (Book Details & Chapters)
* **الحالة:** مكتمل بالكامل بربط حقيقي مع **Firestore**.
* **العمليات المدعومة:**
  * **بيانات الكتاب:** جلب وثيقة الكتاب كاملة من `/books/{bookId}`.
  * **الفصول (Chapters Subcollection):** جلب الفصول والملفات الصوتية التابعة للكتاب ديناميكياً من المجموعة الفرعية `/books/{bookId}/chapters` وترتيبها بحسب حقل الترتيب `orderIndex`.

### 5. مشغل الصوت وتتبع إحصائيات الاستماع (Audio Player & Listening Stats)
* **الحالة:** مكتمل بالكامل بربط حقيقي مع **Firestore**.
* **العمليات المدعومة:**
  * **حساب دقائق الاستماع:** عند تشغيل أي كتاب صوتي، يتم تشغيل مؤقت يضيف دقيقة استماع في Firestore كل 60 ثانية استماع حقيقية، مع توزيعها على أيام الأسبوع الحالية (`weeklyActivityMinutes`) ضمن عملية موحدة (Firestore Transaction).
  * **الكتب المستمع إليها:** عند تشغيل كتاب لأول مرة، يضاف سجله في `/users/{uid}/listened_books/{bookId}` ويُرفع إجمالي الكتب المستمع إليها (`totalBooksListened`) بمقدار 1.
  * **روابط التشغيل:** يتم تشغيل الملفات الصوتية مباشرة من الروابط المخزنة في وثائق الفصول بـ Firestore.

### 6. قارئ النصوص الذكي والنصوص المتزامنة (Book Reader & AI Transcripts)
* **الحالة:** مكتمل بالكامل بالربط بين **Firestore** وموديل **Gemini AI** الحقيقي.
* **العمليات المدعومة:**
  * **فحص الكاش:** يقوم التطبيق بالبحث عن النص المتزامن المولد سابقاً للفصل في حقل `transcript` داخل وثيقة الفصل بـ Firestore.
  * **توليد النصوص بالذكاء الاصطناعي (Gemini 2.5 Flash):** في حال عدم وجود نص متزامن، يقوم التطبيق بتحميل الملف الصوتي للفصل، ثم رفعه مباشرة إلى نموذج Gemini مع أمر (Prompt) لتوليد نص عربي متزامن متطابق مع مخطط JSON دقيق (يحتوي النصوص والبداية والنهاية الزمنية لكل جملة).
  * **حفظ الكاش:** بمجرد نجاح Gemini في توليد النص، يتم حفظه فوراً في Firestore لإتاحته بسرعة للقراء الآخرين لاحقاً.

---

## 🔴 ثانياً: الأجزاء التي لا تزال تعمل ببيانات وهمية (Mocked Features)

تستخدم الموديولات التالية حالياً بيانات محلية ومؤقتات لمحاكاة الاستجابة (`Future.delayed`):

### 1. تصفح المكتبة الصوتية (Audio Library / Browse)
* **حالة الربط:** وهمي (Mock).
* **التفاصيل:** يتم جلب التصنيفات وقائمة الكتب من مصفوفات برمجية ثابتة (`AudioCategory.mockCategories` و `AudioStory.mockList`) مع محاكاة تأخير شبكة مدته 600 مللي ثانية.

### 2. مجتمع التطبيق وجلسات القراء (Reader Sessions / Social Feed)
* **حالة الربط:** وهمي (Mock).
* **التفاصيل:** يتم تحميل المنشورات وصور القراء النشطين من ملف `ReaderFeedMockData`. العمليات كالإعجاب، التعليق، الرد، وإضافة منشور جديد تتم في الذاكرة المؤقتة للتطبيق فقط وتختفي عند إعادة تشغيله.

### 3. البودكاست (Podcasts)
* **حالة الربط:** وهمي (Mock).
* **التفاصيل:** يتم تحميل الحلقات من `PodcastMockData.buildEpisodes()` والتعليقات والردود تدار محلياً دون حفظها في أي قاعدة بيانات.

### 4. الريلز والمقاطع الصوتية (Reels)
* **حالة الربط:** وهمي (Mock).
* **التفاصيل:** يتم توليد عناصر المقطع عشوائياً باستخدام `Reel.mock(index)` ومؤشرات الإعجاب والمشاهدة تدار محلياً في الذاكرة.

### 5. الاشتراكات وعمليات الدفع (Subscriptions & Payments)
* **حالة الربط:** وهمي (Mock).
* **التفاصيل:** خطط الاشتراك تجلب من `SubscriptionMockData.plans`. بوابات الدفع (Apple Pay، Google Play In-App Purchase، الدفع بالبطاقات الائتمانية، ومحفظة فودافون كاش) تحاكي عملية الدفع بانتظار لمدة ثانيتين ثم ترجع نتيجة نجاح وهمية في وضع التطوير (Debug Mode) ولكنها غير مرتبطة بأي API حقيقي.

---

## 📊 جدول ملخص الموديولات (Features Integration Summary)

| الموديول (Feature) | حالة الربط (Status) | خدمة Firebase المستخدمة (Firebase/API Service) |
| :--- | :---: | :--- |
| **المصادقة والتسجيل (Auth)** | 🟢 حقيقي (Real) | Firebase Auth, Cloud Firestore, Firebase Storage |
| **الشاشة الشخصية (Profile)** | 🟢 حقيقي (Real) | Cloud Firestore, Firebase Storage, Firebase Auth |
| **الرئيسية والتوصيات (Home)** | 🟢 حقيقي (Real) | Cloud Firestore, Firebase Auth |
| **تفاصيل الكتب والفصول (Book Details)** | 🟢 حقيقي (Real) | Cloud Firestore |
| **تتبع دقائق الاستماع (Listening Tracker)** | 🟢 حقيقي (Real) | Cloud Firestore (Transactions) |
| **قارئ النصوص الذكي (AI Transcript)** | 🟢 حقيقي (Real) | Cloud Firestore, Google Generative AI (Gemini 2.5) |
| **تصفح المكتبة (Audio Library)** | 🔴 وهمي (Mock) | *بيانات محلية ثابتة* |
| **البودكاست (Podcasts)** | 🔴 وهمي (Mock) | *بيانات محلية ثابتة* |
| **مجتمع جلسات القراء (Sessions Feed)** | 🔴 وهمي (Mock) | *بيانات محلية ثابتة* |
| **الريلز (Reels)** | 🔴 وهمي (Mock) | *بيانات محلية ثابتة* |
| **الاشتراكات والدفع (Subscription/Payment)** | 🔴 وهمي (Mock) | *محاكاة دفع محلية* |
